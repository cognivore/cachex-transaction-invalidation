defmodule CachexTransactionInvalidationTest do
  use ExUnit.Case
  doctest CachexTransactionInvalidation

  test "greets the world" do
    assert CachexTransactionInvalidation.hello() == :world
  end
end
